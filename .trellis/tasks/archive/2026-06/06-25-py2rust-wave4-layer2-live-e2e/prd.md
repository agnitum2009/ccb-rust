# Wave 4 Layer 2: live e2e（真实 tmux + 真实 provider CLI）

## Goal

Live real-environment e2e: real tmux + real provider CLIs (codex/claude/gemini/opencode) + real socket. Staged S1 (infra smoke) -> S2 (single provider) -> S3 (multi + recovery) -> S4 (edge).

## Requirements

- TBD

## Acceptance Criteria

- [ ] TBD

## Notes

- Keep `prd.md` focused on requirements, constraints, and acceptance criteria.
- Lightweight tasks can remain PRD-only.
- For complex tasks, add `design.md` for technical design and `implement.md` for execution planning before `task.py start`.
