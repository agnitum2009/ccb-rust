# Rust Functional Parity Owner Plan — Python 7.6.19

> Scope correction 2026-06-27: this file is future 7.6.x intake analysis only. Current Rust parity target remains Python 7.5.2; do not use this as the active parity blocker list.

## Boundary

This is an owner-method risk/work list, not a confirmed-owner registry.

- Python evidence: `/home/agnitum/ccb-git` tag `v7.6.19` (`075eb71f`)
- Rust evidence: `/home/agnitum/ccb` HEAD `80210c20`
- Current Rust main claim that remains safe: 7.5.2-oriented parity line
- Desired future claim: functional parity with Python 7.6.19

Owner-method rule used:

```text
source evidence != owner truth
implementation surface != closed owner gate
functional parity claim requires receipt/test per consequence-bearing surface
```

## Evidence snapshot

- Python `v7.5.2..v7.6.19` has major changes in tests, `lib`, provider backends, mobile gateway, rolepack system, callback safety, runtime performance, and docs/plans.
- Python daemon handlers: 27.
- Rust daemon handlers: 33.
- Python-only daemon handler: `project_sidebar_click`.
- Python provider backends: 17, including `zai`.
- Rust provider backends: 16, missing `zai`.
- Python CLI subcommands include `mobile`, `wait-any`, `wait-all`, `wait-quorum`.
- Rust CLI has one `wait` command shape and no `mobile` command.

## Required work to claim full functional parity

| Priority | Owner surface | Anchor | Current gap | Work required | Gate to close |
| --- | --- | --- | --- | --- | --- |
| P0 | source-intake policy | Python 7.6.19 as target | Rust is still primarily 7.5.2-aligned | Decide and record that 7.6.19 is the next parity target, with explicit non-goals | Owner receipt/ADR: target version + excluded features |
| P0 | provider capability/interface | `zai` provider | Python has `provider_backends/zai`; Rust has no Z.ai provider | Add Rust provider manifest, launcher, session binding, readback/completion adapter, cleanup/runtime tests | Provider parity tests: catalog + launch cmd + session + completion/readback |
| P0 | daemon/sidebar interface | `project_sidebar_click` | Python daemon exposes handler; Rust has focus ops but no same op | Trace Python click payload and Rust sidebar flow; either add compatible op or document equivalent path | Wire-protocol test: sidebar click -> window/agent focus behavior |
| P0 | mobile/remote control | `ccb mobile` / `mobile_gateway` | Python has mobile gateway + Cloudflare/Tailnet/relay paths; Rust has none | Decide in-scope vs Python-only. If in-scope: port CLI, pairing, registry, relay/websocket/service security boundary | CLI tests + service tests + explicit security/credential owner receipt |
| P1 | callback continuation safety | callback identity/action gate | Python 7.6.x added callback-continuation safety plan and fixes | Freeze callback chain scenarios and compare Rust dispatcher/reply delivery semantics | End-to-end ask callback/reply safety tests; no false sender/target identity |
| P1 | rolepack lifecycle policy | current-store/restart adoption | Python has decision 007 and rolepack current-store changes; Rust has rolepack implementation but no 7.6.19 receipt | Compare current pointer, lock, install/update/sync/restart adoption behavior | Targeted rolepack tests matching Python latest decision |
| P1 | wait CLI compatibility | `wait-any/all/quorum` aliases | Python exposes 3 wait subcommands; Rust exposes one `wait` shape | Add aliases or document accepted CLI divergence | Parser/render tests for Python-compatible command forms |
| P1 | runtime performance capability | runtime accelerator / hotpath | Python+legacy have accelerator sidecar; Rust main has different architecture and no intake decision | Decide shared crate/no-intake. If shared, extract portable parser/wait pieces without `.ccb` assumptions | Performance parity receipt: Rust keeps equal/better behavior without Python hot loops |
| P1 | provider catalog vs execution gate | provider registry relationship | Rust can list/provider-register separately from executable support | Keep per-provider gates split: manifest, launcher, session, readback, cleanup | Matrix test proves every advertised provider has execution support or is explicitly disabled |
| P2 | managed tool/workbench | rich workbench / neovim/tool windows | Python 7.6.x has managed-tool-window and rich workbench changes | Classify as required UI parity or Python-only | Tool-window smoke or out-of-scope receipt |
| P2 | Windows/WezTerm native path | Windows native plan | Python has Windows/WezTerm planning/demo artifacts | Decide whether Rust parity includes Windows-native path beyond current tmux/WSL behavior | Platform matrix owner receipt + smoke tests if in-scope |
| P2 | install/update/release flow | npm/release wrappers | Python changed npm/release/install workflows and wrapper scripts | Compare Rust release builder/install outputs against Python package behavior | Install/update smoke on supported platforms |
| P2 | Python/Rust helper family | jsonl/storage/project-view/native-output helpers | Python 7.6.19 includes performance helper roadmap/results | Decide which helpers are superseded by Rust main crates vs need intake | Per-helper parity decision: reused, superseded, or not applicable |

## Minimal execution order

1. Close P0 target-version decision first. Without this, every diff can be argued forever.
2. Close P0 hard user-facing gaps: `zai`, `project_sidebar_click`, `mobile` decision.
3. Close P1 safety/lifecycle surfaces: callback continuation, rolepack current store, wait aliases.
4. Only then spend time on P2 platform/tooling/performance helper parity.

## Non-claims

- This does not say all Python 7.6.19 features must enter `ccbr`.
- This does not merge `ccb-legacy` into `ccbr`.
- This does not treat Python source as owner truth.
- This does not confirm any actual human owner.
