interface:
  display_name: "CCB GitHub Release Maintainer"
  short_description: "Audit CCB release docs and GitHub state"
  default_prompt: "Use $ccb-github to edit, commit, push, merge to main when needed, tag/release when requested, and run the release checker until the GitHub-facing result is verified."
